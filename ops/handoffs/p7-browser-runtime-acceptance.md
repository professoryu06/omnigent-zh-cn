# P7: 浏览器运行验收

**日期**：2026-07-17

## 环境

- 隔离构建服务：`http://127.0.0.1:6768/`
- 构建来源：`E:\agent team\Omnigent\source-zh-cn\web`
- 浏览器：Codex In-app Browser

## 已验证

1. 桌面新会话页正常加载，中文文案包括“会话”“新建会话”“有什么可以帮你的？”“描述任务以开始新会话…”“添加附件”“开始会话”“工作目录”。
2. 设置页正常加载，侧栏、外观设置、主题、终端主题、工作区面板、字体与字号均显示中文。
3. 390 x 844 移动视口下新会话页正常显示；标题、输入框、工具图标和模型选择区域没有重叠或文本截断。
4. 浏览器控制台 error 数为 0。

## 未验证的路径

- 当前隔离服务未提供可用 Agent/Host，因此无法提交并执行真实会话。
- 因而未能在浏览器中端到端验证：真实 CLI 流式输出、终端会话、文件浏览/编辑、工具卡、MCP 启动状态、审批流和运行时语言切换。

这些属于 Omnigent Host 与 CLI 可用后的下一轮集成验收，不影响当前中文构建的静态页面验收结论。

## 相关验证

- P6D 修正后：6 个测试文件、106 项通过。
- P6 最终构建：`npm.cmd run build` 通过。
- 已知全量测试基线：仅 `AgentInfo.test.tsx` 的 `1K` 格式化失败；本轮未重新执行全量测试。
